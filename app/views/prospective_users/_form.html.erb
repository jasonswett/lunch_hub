<%= form_for(@prospective_user, role: "form") do |f| %>
  <% if @prospective_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prospective_user.errors.count, "error") %> prohibited this prospective_user from being saved:</h2>

      <ul>
      <% @prospective_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :first_name, placeholder: "First name" %>
  </div>
  <div class="field">
    <%= f.text_field :email, placeholder: "Email" %>
  </div>
  <div class="actions">
    <%= f.submit "Get Notified", class: "btn btn-danger" %>
  </div>
<% end %>
